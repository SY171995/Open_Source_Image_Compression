# Git Log

Show a formatted recent commit history.

```bash
cd /home/chander/CODE_BASE/libjpeg-turbo && git log --oneline --graph --decorate -20
```

Present the log in a readable summary, highlighting any notable commits or branch structure.
