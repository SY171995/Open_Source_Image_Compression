# Git Status

Show a summary of the current working tree state.

```bash
cd /home/chander/CODE_BASE/libjpeg-turbo && git status && git diff --stat
```

Summarize:
- Which files are staged, modified, or untracked
- Whether the branch is ahead/behind its remote
- Any uncommitted changes worth noting
