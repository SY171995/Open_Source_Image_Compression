# Configure the build

Run CMake configuration from the `build/` directory. Use this when CMakeLists.txt changes or setting up for the first time.

```bash
cd /home/chander/CODE_BASE/libjpeg-turbo/build && cmake .. -G"Unix Makefiles"
```
